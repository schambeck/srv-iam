spring:
  main:
    banner-mode: off
  spring:
    flyway:
      enabled: true
  datasource:
    jdbc-url: jdbc:h2:mem:iam
    username: sa
    password: ''
  h2:
    console.enabled: true

springdoc:
  swagger-ui:
    use-root-path: true

